[phases.build]
cmds = [
  "npm ci",
  "esbuild server/api-only.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/api-server.js"
]

[phases.start]
cmd = "node dist/api-server.js"

[variables]
NODE_ENV = "production"